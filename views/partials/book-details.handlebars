<section class="card">
  <a href="/book/{{id}}">
    <header>{{title}}</header>

    <div class="card-body">
      <img class = "w-12 h-auto rounded-full" src="/images/book-{{id}}.jpg" alt="{{description}}">
      <p>By: {{author_first}} {{author_last}}</p>
    </div>
  </a>
</section>