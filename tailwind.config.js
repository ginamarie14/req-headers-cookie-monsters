/** @type {import('tailwindcss').Config} */
module.exports = {
    content: ["./src/**/*.{html,js}"], //we will need to put a real address here after
    theme: {
      extend: {},
    },
    plugins: [],
  }